{
  "$schema": "https://json.schemastore.org/package.json",
  "scripts": {
    "dev": "nx run-many -t dev --parallel 9",
    "build": "nx run-many -t build",
    "build:backend": "nx run-many -t build -p @grovine/backend",
    "build:docs": "nx run-many -t build -p @grovine/docs",
    "lint": "nx run-many -t lint",
    "test": "nx run-many -t test"
  },
  "dependencies": {
    "@hono/node-server": "^1.14.4",
    "@hono/zod-validator": "^0.4.3",
    "@t3-oss/env-core": "^0.12.0",
    "hono": "^4.7.11",
    "http-status-codes": "^2.3.0",
    "true-myth": "^7.4.0",
    "tsx": "^4.20.2",
    "ulidx": "^2.4.1",
    "zod": "^3.25.63"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.1.3",
    "@nx/vite": "20.8.0",
    "@nx/web": "20.8.0",
    "@vitest/ui": "^3.2.3",
    "dotenv-cli": "^8.0.0",
    "nx": "20.8.0",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vitest": "^3.2.3"
  },
  "pnpm": {
    "overrides": {
      "esbuild@<=0.24.2": ">=0.25.0",
      "tmp@<=0.2.3": ">=0.2.4"
    }
  }
}
